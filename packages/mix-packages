#!/usr/bin/env bash 

# use --force to run non-interactively ( no -y option )

# Hex package manager
mix local.hex --force

# Phoenix
mix archive.install https://github.com/phoenixframework/archives/raw/master/phoenix_new.ez --force

