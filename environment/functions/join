# Thanks to: http://stackoverflow.com/a/17841619/1638091
# join , args
function join()
{
	# Set expansion character
	local IFS="$1"

	# shift past the first argument (the separator)
	shift

	# Expand args to "$1c$2c..." where c is first character if IFS
	echo "$*"
}
