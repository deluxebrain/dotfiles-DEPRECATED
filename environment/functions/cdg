# Change to root directory of current git repository
function cdg()
{
	local git_root=$(findup .git)
	if [ "${git_root}" -a -d "${git_root}" ]; then 
		cd "${git_root}"
		return
	fi

	false
}

export -f cdg && readonly -f cdg
